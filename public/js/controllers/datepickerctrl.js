/*
 * TODO
 *
 * I moved all the stuff from this file into rideListCtrl, 
 * so it's all in one spot. For some reason, when I delete
 * this useless file, I get a javascript error. 
 *
 * I couldn't find any references to the DatepickerCtrl
 * anywhere in the project, so I have no idea what may
 * be causing this. 
 *
 */
angular.module('myApp.controllers')
  .controller('DatepickerDemoCtrl', function($scope) {
  });
